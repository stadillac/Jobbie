@using Jobbie.Web.Models;

@model SpecialtyEditViewModel
@{
    ViewData["Title"] = "Edit Specialty";
}
@using (Html.BeginForm())
{
    <div class="form-horizontal">
        <h2>Specialty</h2>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.Id)

        <div class="row">
            <div class="col">
                <div class="form-group">
                    @Html.LabelFor(model => model.ExpertiseId, htmlAttributes: new { @class = "control-label" })
                    @Html.DropDownListFor(model => model.ExpertiseId, Model.Expertises, Jobbie.Web.Constants.Constants.EmptySelectOptionText, new { @class = "form-control" })
                    @Html.ValidationMessageFor(model => model.ExpertiseId, "", new { @class = "text-danger" })
                </div>
            </div>
            <div class="col">
                <div class="form-group">
                    @Html.LabelFor(model => model.Name, htmlAttributes: new { @class = "control-label" })
                    @Html.EditorFor(model => model.Name, new { htmlAttributes = new { @class = "form-control" } })
                    @Html.ValidationMessageFor(model => model.Name, "", new { @class = "text-danger" })
                </div>
            </div>
        </div>
        <div class="row">
            <div class="col">
                <div class="form-group">
                    <input type="submit" value="Save" class="btn btn-primary" />
                    <a class="btn btn-primary" asp-area="Admin" asp-controller="Specialties" asp-action="Index">Cancel</a>
                </div>
            </div>
        </div>
    </div>
}